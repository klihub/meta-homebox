# This does not matter ATM, I don't need/want and SDK built.
SDK_VENDOR = "-homeboxsdk"
SDK_VERSION := "homebox.0"
SDK_NAME = "${DISTRO}-${TCLIBC}-${SDK_ARCH}-${IMAGE_BASENAME}-${TUNE_PKGARCH}"
SDK_EXT_TYPE = "minimal"
